.DS_Store
build/
.gradle/
local.properties
*.xcuserdatad
.build/
Pods/
node_modules/
.dart_tool/

# Regenerated by any Gradle run / Xcode open.
.kotlin/
.swiftpm/

# Built by `npm run build` / prepublishOnly — never committed.
ionic/dist/

# A published package pins nothing — pub.dev resolves from pubspec.yaml.
# Only applications commit a lockfile.
pubspec.lock
